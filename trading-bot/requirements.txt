# Core analysis
pandas>=2.0.0
numpy>=1.26.0
ta>=0.11.0
python-dotenv>=1.0.0

# HTTP — public Binance API + Telegram (no SDK, no API keys)
httpx>=0.27.0

# MCP server for Claude Desktop integration
mcp[cli]>=1.0.0

# Standalone scheduler mode
schedule>=1.2.0

# Optional: standalone AI mode without Claude Desktop
# Requires ANTHROPIC_API_KEY to be set in .env
anthropic>=0.40.0
